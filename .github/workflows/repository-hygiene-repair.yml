name: Repository Hygiene Repair

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Manual-only while cleanup script is being narrowed"
